-- AUTOGENERATED: do not edit by hand.
-- Generated from conformance/fixtures/CV-DA-FEE-FLOOR.json via tools/formal/gen_lean_conformance_vectors.py

set_option maxHeartbeats 10000000
set_option maxRecDepth 50000

namespace RubinFormal.Conformance

structure CVDaFeeFloorVector where
  id : String
  op : String
  txHex : Option String
  expectOk : Bool := true
  expectErr : Option String := none
  fee : Option Nat
  weight : Option Nat
  daBytes : Option Nat
  currentMempoolMinFeeRate : Nat
  minDaFeeRate : Nat
  daSurchargePerByte : Nat
  expectAdmit : Bool
  expectAdmitClass : String
  expectDominantFloor : String
  expectRejectReason : Option String
  expectRelayFeeFloor : Option Nat
  expectDaFeeFloor : Option Nat
  expectDaSurcharge : Option Nat
  expectDaRequiredFee : Option Nat
  expectRequiredFee : Option Nat

def cvDaFeeFloorVectors : List CVDaFeeFloorVector := [
  { id := "CV-DA-FEE-FLOOR-01-DA-EXACT", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := some 2000, weight := some 1128, daBytes := some 10, currentMempoolMinFeeRate := 1, minDaFeeRate := 200, daSurchargePerByte := 0, expectAdmit := true, expectAdmitClass := "accepted", expectDominantFloor := "da", expectRejectReason := none, expectRelayFeeFloor := some 1128, expectDaFeeFloor := some 2000, expectDaSurcharge := some 0, expectDaRequiredFee := some 2000, expectRequiredFee := some 2000 },
  { id := "CV-DA-FEE-FLOOR-02-DA-UNDER", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := some 1999, weight := some 1128, daBytes := some 10, currentMempoolMinFeeRate := 1, minDaFeeRate := 200, daSurchargePerByte := 0, expectAdmit := false, expectAdmitClass := "rejected", expectDominantFloor := "da", expectRejectReason := some "DA_FEE_BELOW_STAGE_C_FLOOR", expectRelayFeeFloor := some 1128, expectDaFeeFloor := some 2000, expectDaSurcharge := some 0, expectDaRequiredFee := some 2000, expectRequiredFee := some 2000 },
  { id := "CV-DA-FEE-FLOOR-03-DA-ABOVE", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := some 2001, weight := some 1128, daBytes := some 10, currentMempoolMinFeeRate := 1, minDaFeeRate := 200, daSurchargePerByte := 0, expectAdmit := true, expectAdmitClass := "accepted", expectDominantFloor := "da", expectRejectReason := none, expectRelayFeeFloor := some 1128, expectDaFeeFloor := some 2000, expectDaSurcharge := some 0, expectDaRequiredFee := some 2000, expectRequiredFee := some 2000 },
  { id := "CV-DA-FEE-FLOOR-04-RELAY-DOMINANT-UNDER", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := some 3383, weight := some 1128, daBytes := some 10, currentMempoolMinFeeRate := 3, minDaFeeRate := 1, daSurchargePerByte := 0, expectAdmit := false, expectAdmitClass := "unavailable", expectDominantFloor := "relay", expectRejectReason := some "MEMPOOL_FEE_BELOW_ROLLING_MINIMUM", expectRelayFeeFloor := some 3384, expectDaFeeFloor := some 10, expectDaSurcharge := some 0, expectDaRequiredFee := some 10, expectRequiredFee := some 3384 },
  { id := "CV-DA-FEE-FLOOR-05-MIN-PLUS-SURCHARGE-EXACT", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := some 2000, weight := some 1128, daBytes := some 10, currentMempoolMinFeeRate := 1, minDaFeeRate := 1, daSurchargePerByte := 199, expectAdmit := true, expectAdmitClass := "accepted", expectDominantFloor := "da", expectRejectReason := none, expectRelayFeeFloor := some 1128, expectDaFeeFloor := some 10, expectDaSurcharge := some 1990, expectDaRequiredFee := some 2000, expectRequiredFee := some 2000 },
  { id := "CV-DA-FEE-FLOOR-06-MIN-PLUS-SURCHARGE-UNDER", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := some 2099, weight := some 1128, daBytes := some 10, currentMempoolMinFeeRate := 1, minDaFeeRate := 120, daSurchargePerByte := 90, expectAdmit := false, expectAdmitClass := "rejected", expectDominantFloor := "da", expectRejectReason := some "DA_FEE_BELOW_STAGE_C_FLOOR", expectRelayFeeFloor := some 1128, expectDaFeeFloor := some 1200, expectDaSurcharge := some 900, expectDaRequiredFee := some 2100, expectRequiredFee := some 2100 },
  { id := "CV-DA-FEE-FLOOR-07-DA-FLOOR-OVERFLOW", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := none, weight := some 1128, daBytes := some 10, currentMempoolMinFeeRate := 1, minDaFeeRate := 18446744073709551615, daSurchargePerByte := 0, expectAdmit := false, expectAdmitClass := "rejected", expectDominantFloor := "da", expectRejectReason := some "DA_FEE_FLOOR_OVERFLOW", expectRelayFeeFloor := some 1128, expectDaFeeFloor := some 0, expectDaSurcharge := some 0, expectDaRequiredFee := some 0, expectRequiredFee := some 1128 },
  { id := "CV-DA-FEE-FLOOR-08-NON-DA-RELAY-PASS", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := some 419, weight := some 418, daBytes := some 0, currentMempoolMinFeeRate := 1, minDaFeeRate := 200, daSurchargePerByte := 200, expectAdmit := true, expectAdmitClass := "accepted", expectDominantFloor := "relay", expectRejectReason := none, expectRelayFeeFloor := some 418, expectDaFeeFloor := some 0, expectDaSurcharge := some 0, expectDaRequiredFee := some 0, expectRequiredFee := some 418 },
  { id := "CV-DA-FEE-FLOOR-09-RELAY-DOMINANT-BELOW-BOTH", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := some 1999, weight := some 1128, daBytes := some 10, currentMempoolMinFeeRate := 3, minDaFeeRate := 200, daSurchargePerByte := 0, expectAdmit := false, expectAdmitClass := "rejected", expectDominantFloor := "da", expectRejectReason := some "DA_FEE_BELOW_STAGE_C_FLOOR", expectRelayFeeFloor := some 3384, expectDaFeeFloor := some 2000, expectDaSurcharge := some 0, expectDaRequiredFee := some 2000, expectRequiredFee := some 2000 },
  { id := "CV-DA-FEE-FLOOR-10-RELAY-DOMINANT-EXACT", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := some 3384, weight := some 1128, daBytes := some 10, currentMempoolMinFeeRate := 3, minDaFeeRate := 200, daSurchargePerByte := 0, expectAdmit := true, expectAdmitClass := "accepted", expectDominantFloor := "relay", expectRejectReason := none, expectRelayFeeFloor := some 3384, expectDaFeeFloor := some 2000, expectDaSurcharge := some 0, expectDaRequiredFee := some 2000, expectRequiredFee := some 3384 },
  { id := "CV-DA-FEE-FLOOR-11-TIE-UNDER", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := some 1130, weight := some 1131, daBytes := some 13, currentMempoolMinFeeRate := 1, minDaFeeRate := 87, daSurchargePerByte := 0, expectAdmit := false, expectAdmitClass := "rejected", expectDominantFloor := "da", expectRejectReason := some "DA_FEE_BELOW_STAGE_C_FLOOR", expectRelayFeeFloor := some 1131, expectDaFeeFloor := some 1131, expectDaSurcharge := some 0, expectDaRequiredFee := some 1131, expectRequiredFee := some 1131 },
  { id := "CV-DA-FEE-FLOOR-12-RELAY-FLOOR-OVERFLOW", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := some 18446744073709551615, weight := some 1128, daBytes := some 10, currentMempoolMinFeeRate := 18446744073709551615, minDaFeeRate := 1, daSurchargePerByte := 0, expectAdmit := false, expectAdmitClass := "unavailable", expectDominantFloor := "relay", expectRejectReason := some "MEMPOOL_FEE_BELOW_ROLLING_MINIMUM", expectRelayFeeFloor := none, expectDaFeeFloor := some 10, expectDaSurcharge := some 0, expectDaRequiredFee := some 10, expectRequiredFee := some 10 },
  { id := "CV-DA-FEE-FLOOR-13-DA-SURCHARGE-OVERFLOW", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := none, weight := some 1128, daBytes := some 10, currentMempoolMinFeeRate := 1, minDaFeeRate := 1, daSurchargePerByte := 18446744073709551615, expectAdmit := false, expectAdmitClass := "rejected", expectDominantFloor := "da", expectRejectReason := some "DA_SURCHARGE_OVERFLOW", expectRelayFeeFloor := some 1128, expectDaFeeFloor := some 10, expectDaSurcharge := some 0, expectDaRequiredFee := some 0, expectRequiredFee := some 1128 },
  { id := "CV-DA-FEE-FLOOR-14-DA-REQUIRED-FEE-OVERFLOW", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := none, weight := some 1128, daBytes := some 10, currentMempoolMinFeeRate := 1, minDaFeeRate := 922337203685477580, daSurchargePerByte := 922337203685477582, expectAdmit := false, expectAdmitClass := "rejected", expectDominantFloor := "da", expectRejectReason := some "DA_REQUIRED_FEE_OVERFLOW", expectRelayFeeFloor := some 1128, expectDaFeeFloor := some 9223372036854775800, expectDaSurcharge := some 9223372036854775820, expectDaRequiredFee := some 0, expectRequiredFee := some 1128 },
  { id := "CV-DA-FEE-FLOOR-15-ZERO-MIN-SURCHARGE-EXACT", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := some 2000, weight := some 1128, daBytes := some 10, currentMempoolMinFeeRate := 1, minDaFeeRate := 0, daSurchargePerByte := 200, expectAdmit := true, expectAdmitClass := "accepted", expectDominantFloor := "da", expectRejectReason := none, expectRelayFeeFloor := some 1128, expectDaFeeFloor := some 0, expectDaSurcharge := some 2000, expectDaRequiredFee := some 2000, expectRequiredFee := some 2000 },
  { id := "CV-DA-FEE-FLOOR-16-TIE-EXACT", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := some 1131, weight := some 1131, daBytes := some 13, currentMempoolMinFeeRate := 1, minDaFeeRate := 87, daSurchargePerByte := 0, expectAdmit := true, expectAdmitClass := "accepted", expectDominantFloor := "tie", expectRejectReason := none, expectRelayFeeFloor := some 1131, expectDaFeeFloor := some 1131, expectDaSurcharge := some 0, expectDaRequiredFee := some 1131, expectRequiredFee := some 1131 },
  { id := "CV-DA-FEE-FLOOR-17-DA-ZERO-REQUIRED-SKIPS-FEE", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := none, weight := some 1128, daBytes := some 10, currentMempoolMinFeeRate := 0, minDaFeeRate := 0, daSurchargePerByte := 0, expectAdmit := true, expectAdmitClass := "accepted", expectDominantFloor := "none", expectRejectReason := none, expectRelayFeeFloor := some 0, expectDaFeeFloor := some 0, expectDaSurcharge := some 0, expectDaRequiredFee := some 0, expectRequiredFee := some 0 },
  { id := "CV-DA-FEE-FLOOR-18-NON-DA-SKIPS-FEE", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := none, weight := some 418, daBytes := some 0, currentMempoolMinFeeRate := 0, minDaFeeRate := 200, daSurchargePerByte := 200, expectAdmit := true, expectAdmitClass := "accepted", expectDominantFloor := "none", expectRejectReason := none, expectRelayFeeFloor := some 0, expectDaFeeFloor := some 0, expectDaSurcharge := some 0, expectDaRequiredFee := some 0, expectRequiredFee := some 0 },
  { id := "CV-DA-FEE-FLOOR-19-OMITTED-CURRENT-FLOOR-DEFAULT", op := "da_fee_floor_policy", txHex := none, expectOk := true, expectErr := none, fee := some 1127, weight := some 1128, daBytes := some 10, currentMempoolMinFeeRate := 1, minDaFeeRate := 0, daSurchargePerByte := 0, expectAdmit := false, expectAdmitClass := "unavailable", expectDominantFloor := "relay", expectRejectReason := some "MEMPOOL_FEE_BELOW_ROLLING_MINIMUM", expectRelayFeeFloor := some 1128, expectDaFeeFloor := some 0, expectDaSurcharge := some 0, expectDaRequiredFee := some 0, expectRequiredFee := some 1128 },
  { id := "CV-DA-FEE-FLOOR-20-MALFORMED-TX-PARSE-FIRST", op := "da_fee_floor_policy", txHex := some "00", expectOk := false, expectErr := some "TX_ERR_PARSE", fee := none, weight := none, daBytes := none, currentMempoolMinFeeRate := 1, minDaFeeRate := 1, daSurchargePerByte := 1, expectAdmit := false, expectAdmitClass := "", expectDominantFloor := "", expectRejectReason := none, expectRelayFeeFloor := none, expectDaFeeFloor := none, expectDaSurcharge := none, expectDaRequiredFee := none, expectRequiredFee := none }
]


end RubinFormal.Conformance
