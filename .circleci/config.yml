version: 2.1

jobs:
  noop:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: no-op
          command: |
            echo "CircleCI placeholder workflow: no-op, status check kept green"

workflows:
  keepalive:
    jobs:
      - noop
