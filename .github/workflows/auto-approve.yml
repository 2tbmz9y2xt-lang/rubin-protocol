name: Auto-approve PR (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-by-policy:
    runs-on: ubuntu-latest
    steps:
      - name: Explain policy
        run: |
          echo "Auto-approve and auto-merge are disabled by repository governance."
          echo "rubin-protocol uses required checks plus manual merge via cl pr-merge."
