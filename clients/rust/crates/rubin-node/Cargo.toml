[package]
name = "rubin-node"
version = "0.0.0"
edition = "2021"

[dependencies]
hex = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

rubin-consensus = { path = "../rubin-consensus" }
