# Local agent instructions (must not be committed/published)
AGENTS.md

# Internal workflow/governance docs (keep local, do not publish)
INBOX.md
inbox/
AGENT_WORKFLOW_RULES.md
IMPLEMENTATION_ROADMAP.md
SECURITY_BUILD_PROFILES.md

# Service / local scripts (must not be committed/published)
scripts/local/
scripts/private/
*.local.sh
*.local.ps1

# Local chat prompts (must not be committed/published)
PROMPTS_CODERS.md

# Local tool artifacts
.codex/
.vscode/
.idea/
*.log
*.tmp
*.swp
.DS_Store

# Build artifacts (must not be committed)
clients/rust/target/
clients/**/target/
**/*.dSYM/

# Go build artifacts
clients/go/bin/
clients/go/**/*.test
clients/go/gosec_output.json
clients/go/semgrep_output.json

# Rust security tool artifacts
clients/rust/cargo_audit.json

# wolfCrypt shim build artifacts (operator-local)
**/librubin_wc_shim.so
**/librubin_wc_shim.dylib
**/librubin_wc_shim.dll
